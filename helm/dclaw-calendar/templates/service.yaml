apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-calendar.fullname" . }}
  labels:
    {{- include "dclaw-calendar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-calendar.selectorLabels" . | nindent 4 }}
